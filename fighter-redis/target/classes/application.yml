spring:
  # mysql数据库配置
  datasource:
    url: jdbc:mysql://localhost:3306/cule?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: root
    type: com.alibaba.druid.pool.DruidDataSource
  # redis 配置
  redis:
    database: 0
    port: 6379
    host: localhost
    password:
mybatis:
    # 配置别名
    typeAliasesPackage: com.fanlan.fighterredis.pojo
    # 扫描xml
    mapper-locations: classpath:mapper/*.xml



